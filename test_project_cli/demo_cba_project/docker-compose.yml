version: '3.8'

services:
  hub:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./mission_trace.json:/app/mission_trace.json
      - ./report.html:/app/report.html
      - ./screenshots:/app/screenshots
    environment:
      - PWDEBUG=0
    restart: always
